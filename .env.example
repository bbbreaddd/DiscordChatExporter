# Copy to .env (next to compose.yml) and fill in. compose.yml reads these.

# Bot token. Used when watcher.yaml doesn't set `token` / `token-file`.
DISCORD_TOKEN=

# Container timezone for log timestamps (e.g. America/New_York). Defaults to UTC.
TZ=UTC
